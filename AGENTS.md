# Agent instructions

The project instructions live in `CLAUDE.md` at this level (commands, structure, invariants, verification) and in `apps/web/AGENTS.md` for the Next.js 16 specifics that `next dev` regenerates. Read `CLAUDE.md` first; do not duplicate its content here.
